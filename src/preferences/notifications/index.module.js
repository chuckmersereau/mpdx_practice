import component from './notifications.component';
import service from './notifications.service';

export default angular.module('mpdx.preferences.notifications', [
    component,
    service
]).name;