import component from './mailchimp.component';
import service from './mailchimp.service';

export default angular.module('mpdx.preferences.integrations.mailchimp', [
    component,
    service
]).name;