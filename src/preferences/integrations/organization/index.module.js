import component from './organization.component';
import service from './organization.service';

export default angular.module('mpdx.preferences.integrations.organization', [
    component,
    service
]).name;