import controller from './integrations.controller';
import service from './integrations.service';

export default angular.module('mpdx.preferences.integrations.google.integrations', [
    controller,
    service
]).name;
